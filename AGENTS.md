# AGENTS.md - エージェント開発ガイドライン & プロジェクト定義書

このドキュメントは、本プロジェクト（**清水明『熱力学の基礎（第2版）』非公式学習サポート Web アプリ**）において、AI エージェントおよび開発者が遵守すべきアーキテクチャ方針・数学的表記規則・開発ツールチェーン・セキュリティルールを定義したものです。

---

## 1. プロジェクト概要 & 基本方針

- **目的**: 清水明著『熱力学の基礎（第2版）』の学習を補助する静的 Web インタラクティブビジュアライザ。
- **原則**: サーバーを持たない **100% ブラウザ完結型の純粋な静的 Web アプリケーション** (Pure Client-Side Static Web App)。
- **技術スタック**: React 19, TypeScript 7, Vite 8, Vitest 4, KaTeX, Lucide React, Tailwind CSS.

---

## 2. 開発ツールチェーン & パッケージ管理

- **パッケージマネージャー**: 必ず **`pnpm`** を使用すること（`npm` や `yarn` の直接実行は不可）。
- **ツールバージョン管理 (`mise`)**:
  - `mise.toml` および `mise.lock` によって Node.js (`24.19.0`), pnpm (`11.20.0`), Terraform (`1.10.5`) のバージョンが管理されています。
  - ローカル実行時は `mise exec -- pnpm test` や `mise exec -- pnpm build` を使用してください。
- **セキュリティポリシー**:
  - `.npmrc`: `minimum-release-age=7d`（リリース1週間未満のパッケージ使用禁止）および `ignore-scripts=true`（スクリプト無断実行の禁止）。
  - `pnpm-workspace.yaml`: ビルドスクリプトの許可は `esbuild` と `workerd` のみに限定ホワイトリスト登録されています。

---

## 3. 数学的表記ルール & 物理学的厳密性

1. **教科書の略記法に準拠**:
   - ルジャンドル変換の定義は、特異性・不連続領域を考慮し、教科書の表記法 **\(g(p) = [xp - f(x)](p)\)** を正確に使用すること。
2. **KaTeX 数式レンダリング**:
   - インライン数式: `\( ... \)` または `$ ... $`
   - ディスプレイ数式: `\[ ... \]` または `$$ ... $$`
3. **グラフプロットの 1:1 ピクセルアスペクト比**:
   - \(f'(x)\) および \(g'(p)\) のグラフプロットエリアは、縦軸と横軸の長さ 1 あたりのピクセル幅を完全一致（正方形グリッド）させること。

---

## 4. ルーティング & インフラ・CI/CD

- **ルーティング**: GitHub Pages / Cloudflare Pages 等の静的ホスティングで 404 エラーを起こさないよう、`react-router-dom` の **`HashRouter`** (`/#/`, `/#/legendre`) を使用すること。
- **CI/CD パイプライン**:
  - `.github/workflows/deploy.yml` に統合されています。
  - 順序: `1. Test & Build` $\to$ `2. Terraform IaC Apply` $\to$ `3. Deploy to Cloudflare Pages` (via Wrangler).
  - サードパーティ GitHub Actions はサプライチェーン保護のため 40 桁フルコミット SHA で固定すること。

---

## 5. エージェントの行動規範 & 品質保証

1. **技術的検証の徹底 (Verification Before Completion)**:
   - 変更完了を宣言する前に、必ず `pnpm test`（Vitest 単体テスト）および `pnpm build`（Vite プロダクションビルド）を実行し、エラーゼロを確認すること。
2. **盲目的な同調の禁止 (Technical Rigor)**:
   - ユーザーからの指摘や推測に対して適当に同調せず、コードベースおよび公式ドキュメントの実装事実を調査・検証した上で技術的根拠をもって回答すること。
3. **禁止コマンドの遵守**:
   - `sudo`, `rm -rf`, `git push origin main` などの破壊的・不可逆コマンドは絶対に実行しないこと。
